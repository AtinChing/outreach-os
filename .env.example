# Master Ghost DB connection string — run `ghost create` then `ghost connect <id>`
# For local testing, use the docker-compose postgres service:
MASTER_DATABASE_URL=postgresql://ghost:ghostpass@localhost:5432/master_db

GEMINI_API_KEY=
HASDATA_API_KEY=
AUTH0_DOMAIN=
AUTH0_AUDIENCE=
